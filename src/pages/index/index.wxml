<view class="intro">
    <button 
        wx:if="{{canIUse}}" 
        class="auth-btn" 
        open-type="getUserInfo"
        lang="zh_CN"
        bindgetuserinfo="onGetUserInfo"
    >授权登录</button>
    <view wx:else>请升级微信版本</view>
</view>